export interface InputConfig {
  autocomplete?: string;
  icon?: string;
  name: string;
  placeholder?: string;
  type: string;
}

export interface ButtonConfig {
  disabled: boolean;
  text: string;
  type: string;
}

